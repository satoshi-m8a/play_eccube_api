define([], function () {
    'use strict';

    var BlockCtrl = function ($scope, $rootScope) {
    };
    BlockCtrl.$inject = ['$scope', '$rootScope', '$location'];

    return {
        BlockCtrl: BlockCtrl
    };

});